# 1. Reset /etc/resolv.conf symlink back to NetworkManager default
sudo rm -f /etc/resolv.conf
sudo systemctl restart NetworkManager

# 2. Restart warp service
sudo systemctl restart warp-svc
sleep 2

# 3. Connect
warp-cli connect
